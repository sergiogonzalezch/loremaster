# Docker Compose para desarrollo local.
# Para producción usar: docker-compose -f docker-compose.prod.yml up -d
# (requiere variables POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB definidas)

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    # Solo expuerto para desarrollo local. Producción: eliminar ports y usar red interna.
    ports:
      - "127.0.0.1:6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage
    networks:
      - loremaster

  postgres:
    image: postgres:15-alpine
    container_name: postgres
    profiles:
      - postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-loremaster}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-loremaster}
      POSTGRES_DB: ${POSTGRES_DB:-loremaster}
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - loremaster
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-loremaster}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "127.0.0.1:6379:6379"
    networks:
      - loremaster
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  qdrant_data:
  postgres_data:

networks:
  loremaster:
    driver: bridge
