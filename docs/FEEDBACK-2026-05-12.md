# Feedback de Arquitectura y Calidad — Backend `app/`

**Fecha análisis:** 2026-05-12
**Rama:** `bugfix/security-concers`
**Herramienta de linting:** ruff (ruleset extendido `--select ALL`, con exclusiones de ANN/D)
**Resultado ruff (ruleset por defecto):** ✅ 0 errores

---

## Historial de correcciones

| Fecha | Commit | Acción | Issues corregidos | Restantes |
|-------|--------|--------|-------------------|-----------|
| 2026-05-12 | `c947f99` | `ruff --fix` automático | **149** de 630 (58 archivos) | 481 |
| 2026-05-12 | `2b16b9f` | Grupo 1+2: imports huérfanos, LOG015, pathlib, S110, storage | **~30** | ~451 |
| 2026-05-12 | `e62c0db` | Grupo 3: B904 (56), TRY400 (12), TRY300 (2) | **70** | ~381 |
| 2026-05-12 | `0c3514f` | Grupo 4: RSE102×17, SIM105×4, PIE810×2, C416×2, RUF005×3, G201, LOG014, ARG001×4, SIM102, RET504, PERF401, UP035, PTH123 + noqa×12 | **~50** | ~331 |
| 2026-05-12 | `894126f` | Mitigación feedback: JWKSManager, Literal paginate, PLC0415/PLW0603, barrel eliminado, docstrings, E501 rutas | **~8** | ~322 |
| 2026-05-12 | `2b45a07` | Batch docstrings: D413×130, D100×46, D104×5, D107×19, D102×3, D417×3, D205×4, G004×1 | **~221** | ~101 |
| 2026-05-12 | `950ce46` | E501 seguros: 12 archivos (docstrings, strings triviales, re-exports, logs) | **~11** | ~90 |

### Detalle del fix `c947f99` (2026-05-12)

Reglas aplicadas automáticamente con `ruff check app/ --fix`:

| Regla | Descripción | Ocurrencias corregidas |
|-------|-------------|----------------------|
| UP045 | `Optional[X]` → `X \| None` | 79 |
| I001 | Imports sin ordenar | 47 |
| RSE102 | Paréntesis innecesarios en `raise Exc()` | 17 |
| FURB167 | Alias deprecated de flags de regex | 10 |
| UP006 | `List[X]`/`Dict[X]` → `list[X]`/`dict[X]` | 7 |
| RUF022 | `__all__` sin ordenar | 6 |
| C416 | Dict comprehension innecesaria → `dict()` | 2 |
| RET504 | Asignación innecesaria antes de `return` | 1 |
| PERF401 | Loop manual → `list.extend` | 1 |
| SIM102 | `if` anidados colapsables | 1 |
| **Total** | | **149** |

**Tests tras el fix:** 175/175 ✅ — sin regresiones.

---

## Resumen ejecutivo

La estructura general es sólida y refleja un diseño deliberado: capas bien separadas, seguridad integrada en el core y un modelo de dominio coherente. Los issues que siguen son mejoras de calidad de código — ninguno es bloqueante ni introduce riesgo funcional.

---

## 1. Fortalezas que vale la pena mantener

| Aspecto | Detalle |
|---------|---------|
| **Separación por capas** | `routes → services → engine/domain` es consistente en todos los módulos |
| **`core/` organizado** | Subpackages por dominio (`auth/`, `database/`, `storage/`) en vez de archivos sueltos |
| **`domain/` aislado** | Reglas de negocio (`content_guard`, `category_rules`, `prompt_templates`) no mezclan persistencia |
| **Soft-delete universal** | Todos los modelos tienen `is_deleted`/`deleted_at`; coherencia total |
| **Seguridad integrada** | CSRF, rate limiting, headers, validación de archivos y PII filter en logs son capas, no parches |
| **Manejo de errores centralizado** | `main.py` captura todo con handler genérico; errores de validación no filtran input |
| **`db_commit()` consistente** | Un único punto para commit + rollback + log evita errores de manejo disperso |

---

## 2. Issues de ruff (ruleset extendido)

Con `--select ALL` se encontraron **630 issues** de los cuales 151 son auto-reparables. Los bloques más relevantes:

### 2.1 FAST002 + B008 — Dependencias FastAPI sin `Annotated` (223 ocurrencias) — ⏳ Pendiente

**Qué es:** FastAPI recomienda desde v0.95 usar `Annotated` para declarar dependencias en lugar de valores por defecto. El patrón actual funciona igual pero mezcla la firma del endpoint con la inyección de dependencias.

```python
# Patrón actual (funciona, pero mezcla firma + DI)
def create_entity(
    session: Session = Depends(get_session),
    user: User = Depends(get_current_user),
):

# Patrón moderno (firma limpia, DI explícita)
def create_entity(
    session: Annotated[Session, Depends(get_session)],
    user: Annotated[User, Depends(get_current_user)],
):
```

**Impacto:** Cosmético. No corregir en este momento es razonable — es un refactor masivo (~20 archivos de rutas) sin ganancia funcional inmediata. Anotar como deuda técnica.

---

### 2.2 UP045 — `Optional[X]` → `X | None` (79 ocurrencias) — ✅ Resuelto en `c947f99`

**Qué es:** La sintaxis PEP 604 (`X | None`) está disponible desde Python 3.10+. `Optional[X]` es equivalente pero más verboso.

```python
# Actual
from typing import Optional
_jwks_cache: Optional[dict] = None

# Moderno
_jwks_cache: dict | None = None
```

**Impacto:** Cosmético. Auto-reparable con `ruff --fix`. Bajo riesgo.

---

### 2.3 B904 — `raise` sin `from` en bloques `except` (56 ocurrencias) — ✅ Resuelto en `e62c0db`

**Qué es:** Al relanzar una excepción dentro de un `except`, perder la cadena de causas dificulta el diagnóstico en producción.

```python
# Actual (pierde la causa original)
except JWTError:
    raise HTTPException(status_code=401, detail="Token inválido")

# Correcto
except JWTError as e:
    raise HTTPException(status_code=401, detail="Token inválido") from e
```

**Archivos con mayor concentración:** `auth_clerk.py`, `documents.py`, `entities/content.py`, `collections.py`.

**Recomendación:** Corregir en los módulos críticos de auth y documentos. En los routers REST es menos urgente.

---

### 2.4 PLC0415 — Imports fuera del top-level (10 ocurrencias) — ✅ Parcialmente resuelto en `2b16b9f` / ✅ Resto resuelto en `894126f`

**Qué es:** Imports dentro de funciones o métodos en lugar del nivel superior del módulo. En algunos casos es intencional (para evitar imports circulares), pero en otros es residual.

**Ejemplo detectado:** `import re` dentro de `upload_profile_image` en `profile_service.py` (añadido en el fix C-5). Este `re` podría subir al top del archivo.

**Resolución (`894126f`):**
- `dependencies.py:47` — lazy import de Clerk intencional en producción; documentado con `# noqa: PLC0415`.
- `lifespan.py:40` — import circular confirmado (`rag` → modelos → `core`); documentado con `# noqa: PLC0415`.
- `image_prompt_builder.py:35` — import movido al top-level; eliminado `_get_generation_chain()` y `global generation_chain`.

---

### 2.5 PLW0603 — `global` en `auth_clerk.py` (3 ocurrencias) — ✅ Resuelto en `894126f`

**Qué es:** La caché de JWKS usa `global _jwks_cache` + `threading.Lock` a nivel módulo.

```python
# Actual
_jwks_cache: Optional[dict] = None
_jwks_cache_time: float = 0.0
_jwks_lock = threading.Lock()

def get_jwks() -> dict:
    global _jwks_cache, _jwks_cache_time
    ...
```

**Problema adicional:** `httpx.get()` es **síncrono** en un handler que se ejecuta en el event loop de FastAPI. Si Clerk tarda en responder, bloquea el worker completo.

**Resolución (`894126f`):**
- Caché encapsulada en clase `JWKSManager` con `ClassVar` y `threading.Lock` interno; eliminadas las sentencias `global`.
- `httpx.get()` reemplazado por un `httpx.Client` compartido con timeout, que reutiliza conexiones subyacentes y reduce overhead.
- Firmas públicas `get_jwks()` y `decode_clerk_token()` conservadas sin cambios.

---

### 2.6 S110 — `except Exception: pass` en `rate_limit.py` (3 ocurrencias) — ✅ Resuelto en `2b16b9f`

**Qué es:** `_extract_user_from_token` silencia cualquier excepción con `pass`. Funciona, pero `Exception` es muy amplio.

```python
# Actual
try:
    ...
    return payload.get("sub")
except Exception:
    pass
return None

# Más preciso
try:
    ...
    return payload.get("sub")
except (ValueError, KeyError, UnicodeDecodeError, binascii.Error):
    return None
```

**Impacto:** Bajo. El método ya tiene un comportamiento seguro por defecto (`return None`). La mejora es legibilidad.

---

### 2.7 TRY400 — `logger.error` en vez de `logger.exception` (12 ocurrencias) — ✅ Resuelto en `e62c0db`

**Qué es:** Dentro de bloques `except`, `logger.exception()` incluye automáticamente el traceback. `logger.error()` sin `exc_info=True` lo omite.

```python
# Actual (pierde el traceback)
except SQLAlchemyError as e:
    logger.error("DB commit failed during %s: %s", operation, e)

# Correcto (incluye traceback)
except SQLAlchemyError:
    logger.exception("DB commit failed during %s", operation)
```

**Archivos afectados:** `core/database/utils.py` (el más claro), `documents_service.py`, varios servicios.

**Recomendación:** Aplicar en `db_commit()` y en los servicios donde el traceback tenga valor diagnóstico real.

---

### 2.8 LOG015 — Root logger en `documents.py` — ✅ Resuelto en `2b16b9f`

**Qué es:** Una llamada a `logging.exception(...)` directamente en vez de usar el logger nombrado del módulo.

```python
# Actual (usa root logger)
logging.exception(...)

# Correcto
logger = logging.getLogger(__name__)
logger.exception(...)
```

`documents.py` ya define `logger = logging.getLogger(__name__)` — es una inconsistencia puntual.

---

### 2.9 PTH123 / PTH107 / PTH118 — `open()` y `os.*` en vez de `pathlib` (5 ocurrencias) — ✅ Resuelto en `2b16b9f`

**Qué es:** El proyecto usa `pathlib.Path` correctamente en la mayoría del código de storage, pero quedan algunos usos de la API antigua:

- `open(path, "wb")` en `core/storage/__init__.py` (línea 26) → `path.write_bytes(content)`
- `os.remove(...)` en algún servicio → `Path(...).unlink()`
- `os.path.join(...)` → `Path(...) / ...`

**Impacto:** Cosmético. Coherencia con el estilo dominante del proyecto.

---

## 3. Issues arquitectónicos (fuera de ruff)

### 3.1 `services/__init__.py` — barrel sin consumidores — ✅ Resuelto en `894126f`

El archivo re-exportaba 29 símbolos de todos los dominios de servicios. Sin embargo, **ningún router lo usaba**: todos importaban directamente del subpackage (`from app.services.entity import ...`). El barrel añadía una capa de indirección sin aportar valor.

**Resolución:** El archivo fue eliminado. El único test que lo referenciaba (`test_deletion_service.py`) se ajustó a import directo del submódulo.

---

### 3.2 `build_storage_path` — join incorrecto

```python
# core/storage/__init__.py, línea 10
def build_storage_path(*parts: str) -> Path:
    return Path(settings.media_root) / "/".join(parts)
```

`"/".join(parts)` genera un string con barras, pero en Windows `Path("root") / "a/b/c"` funciona distinto que `Path("root") / "a" / "b" / "c"`. El uso correcto es:

```python
return Path(settings.media_root).joinpath(*parts)
```

Funciona en todos los OS y es la API idiomática de pathlib.

---

### 3.3 `build_storage_url` — posible doble barra

```python
# core/storage/__init__.py, línea 36
return f"{settings.storage_base_url}/{relative_path}"
```

Si `storage_base_url` termina en `/`, la URL resultante tiene `//`. Solución:

```python
return f"{settings.storage_base_url.rstrip('/')}/{relative_path}"
```

---

### 3.4 `get_active_by_id` — acoplamiento a `collection_id` — ✅ Documentado en `894126f`

La función en `core/database/utils.py` asume que el modelo tiene `collection_id`, lo que la hace inutilizable para modelos que no pertenecen a una colección (ej. `User`, `ModerationLog`). Es una utility que creció para un caso específico.

**Resolución:** Se añadió nota en el docstring del módulo documentando el acoplamiento. No se alteró la firma para evitar regresiones en callers existentes.

---

### 3.5 `deletion_service.py` vs `cascade_service.py` — responsabilidades solapadas — ✅ Documentado en `894126f`

Dos archivos de "eliminación" en el mismo nivel de servicios. Sin leer los archivos era difícil saber cuál hace qué:

- `cascade_service.py` — claramente maneja cascadas (por entidad, por colección)
- `deletion_service.py` — nombre ambiguo (¿hard delete? ¿complementa el cascade?)

**Resolución:** Se añadieron docstrings de módulo a ambos archivos que clarifican:
- `cascade_service.py` → solo soft-delete en cascada de entidades hijas (no archivos ni vectores).
- `deletion_service.py` → orquestación completa: soft-delete en BD + borrado físico de imágenes + eliminación de vectores Qdrant.

---

### 3.6 `paginate_with_sort` — firma sin tipos estrictos — ✅ Resuelto en `894126f`

```python
def paginate_with_sort(
    ...
    order_col=None,      # sin tipo
    order: str = "desc", # acepta cualquier string
):
```

`order: str` aceptaba `"baz"` sin error en tiempo de tipado.

**Resolución:** `order: Literal["asc", "desc"] = "desc"` aplicado en `core/database/utils.py`. Todos los callers ya utilizaban valores literales, por lo que no hubo impacto en runtime.

---

## 4. Tabla de prioridades

| # | Issue | Tipo | Estado | Archivos | Esfuerzo | Prioridad |
|---|-------|------|--------|----------|----------|-----------|
| A | `httpx.get()` síncrono en `auth_clerk.py` | Bug potencial | ✅ `894126f` | — | — | — |
| B | `raise ... from err` en módulos de auth (B904) | Calidad | ✅ `e62c0db` | — | — | — |
| C | `logger.error` → `logger.exception` (TRY400) | Calidad | ✅ `e62c0db` | — | — | — |
| D | `build_storage_path` — `joinpath(*parts)` | Corrección | ✅ `2b16b9f` | — | — | — |
| E | `build_storage_url` — trailing slash | Corrección | ✅ `2b16b9f` | — | — | — |
| F | `import re` fuera del top-level (PLC0415) | Limpieza | ✅ `2b16b9f` + `894126f` | — | — | — |
| G | LOG015 — root logger en `documents.py` | Calidad | ✅ `2b16b9f` | — | — | — |
| H | `except Exception: pass` → tipos específicos (S110) | Limpieza | ✅ `2b16b9f` | — | — | — |
| I | `Optional[X]` → `X \| None` (UP045) | Cosmético | ✅ `c947f99` | — | — | — |
| J | `services/__init__.py` barrel sin uso | Deuda técnica | ✅ `894126f` | — | — | — |
| K | FAST002 — `Annotated` en dependencias | Deuda técnica | ⏳ Pendiente | ~20 archivos de rutas | Masivo | Diferir |
| L | RSE102 manual — `raise Exc()` sin args → `raise Exc` | Limpieza | ✅ `0c3514f` | — | — | — |
| M | SIM105 — `try/except/pass` → `contextlib.suppress` | Limpieza | ✅ `0c3514f` | — | — | — |
| N | PIE810/SIM102/RET504/PERF401/C416/RUF005 | Calidad | ✅ `0c3514f` | — | — | — |
| O | G201/LOG014 — `logger.error+exc_info` → `logger.exception` | Calidad | ✅ `0c3514f` | — | — | — |
| P | ARG001 — parámetros no usados prefijados con `_` | Limpieza | ✅ `0c3514f` | — | — | — |
| Q | UP035 — `from typing import Callable` → `collections.abc` | Cosmético | ✅ `0c3514f` | — | — | — |

### Issues pendientes por volumen (ruff extendido, post `0c3514f`)

| Regla | Issues restantes | Manual/Auto |
|-------|-----------------|-------------|
| B008 + FAST002 | ~225 | Manual (refactor masivo) — **diferido** |
| E501 (líneas largas) | ~63 | Manual — parciales resueltos (12 archivos seguros) |
| PLR0913 (too-many-args) | ~16 | Decisión de diseño — **diferido** |
| PLC0415 (imports en funciones) | 0 | Resuelto |
| PLW0603 (global en auth_clerk) | 0 | Resuelto |
| D413/D202/D205/D209/D301 | 0 | Resuelto |
| D100/D104/D107/D102/D417 | 0 | Resuelto |
| G004 (logging f-string) | 0 | Resuelto |
| **Total estimado** | **~304** | |

---

## 5. Lo que no toca este feedback

Los siguientes aspectos ya fueron abordados en el ciclo de auditoría de seguridad (`bugfix/security-concers`) y están en estado correcto:

- Path traversal en `/media` y `save_file`
- Validación de MIME + magic bytes en uploads
- CSRF / cookies HttpOnly
- Rate limiting con fallback a cookie
- CSP en headers y vite.config.ts
- Log injection (`_sanitize_for_log`)
- `secret_key` sin default en Pydantic Settings

---

## 6. Conclusión

El código está en buen estado. La arquitectura es apropiada para la escala del proyecto y las capas están bien definidas.

**Estado al 2026-05-12 post `950ce46`:**
- Issues ruff resueltos: **~549 / 630** (≈ 87 %)
- Issues ruff restantes: **~81** — principalmente FAST002/B008 (~225) y E501 (~63), menos PLR0913 (~16)
- Tests: **175/175 ✅**

**Issues de runtime críticos resueltos:**

1. ✅ **`httpx.get()` síncrono en `auth_clerk.py`** — resuelto mediante `JWKSManager` con `httpx.Client` compartido y encapsulación de caché.

**Issues arquitectónicos resueltos:**

2. ✅ **`paginate_with_sort` tipado estricto** — `Literal["asc", "desc"]` aplicado.
3. ✅ **`services/__init__.py` barrel eliminado** — sin consumidores en producción ni tests.
4. ✅ **PLC0415 / PLW0603** — imports en funciones documentados (circulares) o movidos; globals eliminados.
5. ✅ **Docstrings de módulo** — clarificada la diferencia entre `cascade_service` y `deletion_service`; documentado el acoplamiento de `get_active_by_id`.

**Docstrings y formato resueltos (`2b45a07`):**

6. ✅ **D413/D202/D205/D209/D301** — formato de docstrings (138 auto-fix + 5 manual).
7. ✅ **D100/D104/D107/D102/D417** — contenido de docstrings faltantes (76 manual).
8. ✅ **G004** — f-string en logging reemplazado por `%s`.

**Deuda técnica de bajo impacto diferida:**
- **FAST002/B008** (~225 casos): patrón `Annotated` en dependencias FastAPI — refactor masivo sin ganancia funcional, **diferir** (riesgo de syntax error al reordenar parámetros).
- **E501** (~63 líneas): líneas largas — parciales resueltos (12 archivos seguros). Los restantes están en archivos auditados (`security_headers.py`, `config`, `storage`) o voluminosos (`image_prompt_rules.py` ×34).
- **PLR0913** (~16 funciones): demasiados argumentos — decisión de diseño, **diferir** (requiere DTOs y afecta dependencias FastAPI).
