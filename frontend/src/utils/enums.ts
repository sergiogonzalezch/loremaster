export enum DocumentStatus {
  Processing = "processing",
  Completed = "completed",
  Failed = "failed",
}

export enum EntityType {
  Character = "character",
  Scene = "scene",
  Faction = "faction",
  Item = "item",
}

export enum DraftStatus {
  Pending = "pending",
  Confirmed = "confirmed",
  Discarded = "discarded",
}